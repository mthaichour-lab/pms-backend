import{describe,expect,it}from'vitest';import{approveMassExport,assertExportDatasetSafe,exportChecksum,initialExportStatus}from'../../../src/modules/reporting/domain/secure-export.js';
describe('secure report export',()=>{it('requires reinforced approval only for mass export',()=>{expect(initialExportStatus('SINGLE')).toBe('APPROVED');expect(initialExportStatus('BULK')).toBe('PENDING_REINFORCED_APPROVAL');expect(approveMassExport('PENDING_REINFORCED_APPROVAL','BULK','maker','checker')).toBe('APPROVED');expect(()=>approveMassExport('PENDING_REINFORCED_APPROVAL','BULK','maker','maker')).toThrow(/distinct checker/);});it('rejects clear personal data before any format is rendered',()=>{expect(()=>assertExportDatasetSafe({columns:[{key:'customer',label:'Customer',personalData:true}],rows:[{customer:'Alice'}]})).toThrow(/Clear personal data/);const safe={columns:[{key:'customer',label:'Customer',personalData:true}],rows:[{customer:'tok_1234567890abcdef'}]};expect(()=>assertExportDatasetSafe(safe)).not.toThrow();expect(exportChecksum(safe)).toMatch(/^[a-f0-9]{64}$/);});});
