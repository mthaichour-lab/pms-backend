import{describe,expect,it}from'vitest';import{EmptyQuotationBasisError,InsufficientQuotationBasisError,solveQuotationTarget}from'../../../src/modules/profit-calculation/domain/quotation-solver.js';const base={placementAmount:'1000000',availableBasis:'2000000',projectedGrossRatePercent:'6',targetNetRatePercent:'4.5',standardMaxInvestorNisbaPercent:'75',maxTanazulAmount:'10000',availablePerIrrAmount:'5000'};describe('quotation target solver',()=>{it('returns one standard reconciled verdict',()=>expect(solveQuotationTarget(base)).toMatchObject({verdict:'APPROVABLE',governance:'STANDARD_KEY',commercialViability:'BASIS_AND_MARGIN_VIABLE',recommendedInvestorNisbaPercent:'75.000000'}));it('requires ALCO for a reachable non-standard lever',()=>expect(solveQuotationTarget({...base,targetNetRatePercent:'5'})).toMatchObject({verdict:'ALCO_APPROVAL_REQUIRED',governance:'NON_STANDARD_LEVER_REQUIRES_ALCO'}));it('rejects empty and insufficient bases distinctly',()=>{expect(()=>solveQuotationTarget({...base,availableBasis:'0'})).toThrow(EmptyQuotationBasisError);try{solveQuotationTarget({...base,availableBasis:'500000'})}catch(error){expect(error).toBeInstanceOf(InsufficientQuotationBasisError);expect((error as InsufficientQuotationBasisError).maximumCapacity).toBe('500000.00')}})});
