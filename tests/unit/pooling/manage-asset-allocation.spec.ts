import{describe,expect,it,vi}from'vitest';import{ManageAssetAllocation}from'../../../src/modules/pooling/application/manage-asset-allocation.js';const command={allocationId:'17146c36-a0cb-4e0a-b095-60b67c945eb9',assetId:'a1d817e4-657f-475f-a96a-7eecb8f93acc',poolId:'POOL_DZD',percentage:'20',effectiveFrom:'2026-08-29',justification:'Approved pool allocation'};const conditions={activeAnomalies:[]as const,hasPriorAllocation:false,approvalValid:false};describe('ManageAssetAllocation',()=>{it('simulates without persisting',async()=>{const save=vi.fn();const service=new ManageAssetAllocation({allocatedPercentage:async()=>'70',preconditions:async()=>conditions,save,history:vi.fn()});await expect(service.simulate(command)).resolves.toMatchObject({remainingPercentage:'10.000000',simulated:true,executable:true});expect(save).not.toHaveBeenCalled()});it('isolates an asset with a Sharia anomaly',async()=>{const service=new ManageAssetAllocation({allocatedPercentage:async()=>'0',preconditions:async()=>({activeAnomalies:['SHARIA'],hasPriorAllocation:false,approvalValid:false}),save:vi.fn(),history:vi.fn()});await expect(service.allocate(command,'allocation:1234567890')).rejects.toThrow('isolated')});it('requires an approval for reallocation',async()=>{const service=new ManageAssetAllocation({allocatedPercentage:async()=>'20',preconditions:async()=>({activeAnomalies:[],hasPriorAllocation:true,approvalValid:false}),save:vi.fn(),history:vi.fn()});await expect(service.allocate(command,'allocation:1234567890')).rejects.toThrow('approved')})});
