import{readFile}from'node:fs/promises';const evidence=JSON.parse(await readFile(process.argv[2]??'deploy/dr/results/latest-failover-drill.json','utf8'));const failures=[];if(evidence.story!=='14.2')failures.push('wrong story');for(const name of['core','token-vault']){const result=evidence.services.find(item=>item.name===name);if(!result)failures.push(`missing ${name}`);else{if(result.rpoMs>evidence.targets.rpoMs)failures.push(`${name} RPO exceeded`);if(result.rtoMs>evidence.targets.rtoMs)failures.push(`${name} RTO exceeded`);if(!result.sourceWatermark||!result.recoveredWatermark)failures.push(`${name} watermark missing`)}}if(!evidence.nextTechnicalRestoreDueAt||!evidence.nextFullDrDueAt)failures.push('periodic replay dates missing');if(failures.length){console.error(failures.join('\n'));process.exitCode=1}else console.log('Core and Token Vault failover evidence satisfies RPO/RTO and periodic replay controls.');
