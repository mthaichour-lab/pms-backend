import{forecastPoolYield,type YieldObservation}from'../domain/historical-yield-forecast.js';
export interface HistoricalForecastRepository{generateAtomically(poolId:string,actorId:string,idempotencyKey:string,compute:(values:readonly YieldObservation[])=>ReturnType<typeof forecastPoolYield>):Promise<{forecastId:string}&ReturnType<typeof forecastPoolYield>>;}
export class GenerateHistoricalYieldForecast{constructor(private readonly repository:HistoricalForecastRepository){}execute(poolId:string,actorId:string,idempotencyKey:string){if(!/^[A-Za-z0-9._:-]{2,64}$/.test(poolId))throw new TypeError('Invalid forecast pool identifier');if(!actorId.trim()||idempotencyKey.length<16)throw new TypeError('Forecast actor and idempotency key are required');return this.repository.generateAtomically(poolId,actorId,idempotencyKey,values=>forecastPoolYield(values,12));}}
