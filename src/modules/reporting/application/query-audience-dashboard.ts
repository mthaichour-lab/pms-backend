import{assertDashboardCompliance,type DashboardAudience,type DashboardItem}from'../domain/audience-dashboard.js';
export interface AudienceDashboardRepository{load(audience:DashboardAudience,poolId?:string):Promise<{businessDate?:string;generatedAt:string;items:readonly DashboardItem[];queryDurationMs:number}>;}
export class QueryAudienceDashboard{constructor(private readonly repository:AudienceDashboardRepository){}async execute(audience:DashboardAudience,poolId?:string){if(!['EXECUTIVE','FINANCE','RISK_ALM','SHARIA'].includes(audience))throw new TypeError('Unsupported dashboard audience');if(poolId!==undefined&&!/^[A-Za-z0-9._:-]{2,64}$/.test(poolId))throw new TypeError('Invalid dashboard pool identifier');const result=await this.repository.load(audience,poolId);if(result.queryDurationMs>=3000)throw new Error('Usual dashboard exceeded the 3 second performance budget');return{...result,...assertDashboardCompliance(audience,result.items),performanceBudgetMs:3000};}}
