import{buildTenorYieldCurve,type TenorGap,type TenorObservation}from'../domain/tenor-yield-curve.js';
export interface TenorCurveSource{poolId:string;businessDate:string;currency:string;closingId:string;observations:readonly TenorObservation[];gaps:readonly TenorGap[];}
export interface TenorCurveRepository{latestClosed(poolId:string,customerToken?:string):Promise<TenorCurveSource|undefined>;}
export class QueryTenorYieldCurve{constructor(private readonly repository:TenorCurveRepository){}async execute(poolId:string,customerToken?:string){if(!/^[A-Za-z0-9._:-]{2,64}$/.test(poolId))throw new TypeError('Invalid pool identifier');if(customerToken!==undefined&&!/^tok_[A-Za-z0-9_-]{16,}$/.test(customerToken))throw new TypeError('Invalid customer token');const source=await this.repository.latestClosed(poolId,customerToken);if(!source)throw new Error('No closed reporting period found for this scope');return{poolId:source.poolId,businessDate:source.businessDate,currency:source.currency,closingId:source.closingId,scope:customerToken?'CUSTOMER':'POOL',points:buildTenorYieldCurve(source.businessDate,source.observations,source.gaps)};}}
