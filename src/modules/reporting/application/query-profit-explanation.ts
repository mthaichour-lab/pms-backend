import{renderProfitExplanation,type ProfitExplanationOutput}from'../../profit-calculation/domain/profit-explanation.js';
export interface ProfitExplanationRepository{published(runId:string,accountId:string):Promise<{output:ProfitExplanationOutput;outputChecksumSha256:string}|undefined>;}
export class QueryProfitExplanation{constructor(private readonly repository:ProfitExplanationRepository){}async execute(runId:string,accountId:string,view:'DETAILED'|'SIMPLIFIED'){const uuid=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;if(!uuid.test(runId)||!uuid.test(accountId))throw new TypeError('Explanation identifiers must be UUIDs');if(!['DETAILED','SIMPLIFIED'].includes(view))throw new TypeError('Unsupported explanation view');const record=await this.repository.published(runId,accountId);if(!record)throw new Error('Published profit explanation not found');return{runId,accountId,outputChecksumSha256:record.outputChecksumSha256,...renderProfitExplanation(record.output,view)};}}
