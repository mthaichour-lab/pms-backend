import{InvestmentSubscription,type InvestmentSubscriptionState,type SubscriptionEvent}from'../domain/investment-subscription.js';
export interface InvestmentSubscriptionRepository{find(accountId:string):Promise<InvestmentSubscriptionState|undefined>;save(state:Readonly<InvestmentSubscriptionState>,events:readonly SubscriptionEvent[],idempotencyKey:string):Promise<void>}
export interface CustomerProfitRightsPolicy{assertProfitRightsOperationAllowed(customerId:string):Promise<void>}
export type SubscriptionAction={type:'START'|'ACCEPT'|'ACTIVATE'|'DEPOSIT'|'WITHDRAW'|'BLOCK'|'UNBLOCK'|'RENEW'|'MATURE'|'SUCCESSION'|'CLOSE';businessDate:string;actorId:string;amount?:string;reason?:string;maturityDate?:string;caseReference?:string;acceptedAt?:string;nonGuaranteeAccepted?:boolean;profitSharingMethodAccepted?:boolean};
export class ManageInvestmentSubscription{constructor(private readonly repository:InvestmentSubscriptionRepository,private readonly rights:CustomerProfitRightsPolicy){}async create(input:Omit<InvestmentSubscriptionState,'status'|'openedOn'|'closedOn'|'acceptance'>,actorId:string,idempotencyKey:string){const account=InvestmentSubscription.presimulate(input);await this.repository.save(account.snapshot(),[{type:'SIMULATED',businessDate:new Date().toISOString().slice(0,10),actorId,details:{}}],idempotencyKey);return account.snapshot()}async act(accountId:string,action:SubscriptionAction,idempotencyKey:string){const state=await this.repository.find(accountId);if(!state)throw new Error(`Investment subscription not found: ${accountId}`);if(['ACTIVATE','DEPOSIT','WITHDRAW','RENEW','MATURE','SUCCESSION','CLOSE'].includes(action.type))await this.rights.assertProfitRightsOperationAllowed(state.customerId);const account=InvestmentSubscription.restore(state);switch(action.type){case'START':account.startSubscription(action.businessDate,action.actorId);break;case'ACCEPT':account.acceptTerms({acceptedAt:action.acceptedAt??'',acceptedBy:action.actorId,nonGuaranteeAccepted:action.nonGuaranteeAccepted===true,profitSharingMethodAccepted:action.profitSharingMethodAccepted===true});break;case'ACTIVATE':account.activate(action.businessDate,action.actorId);break;case'DEPOSIT':account.deposit(action.businessDate,action.actorId,action.amount??'');break;case'WITHDRAW':account.withdraw(action.businessDate,action.actorId,action.amount??'');break;case'BLOCK':account.block(action.businessDate,action.actorId,action.reason??'');break;case'UNBLOCK':account.unblock(action.businessDate,action.actorId,action.reason??'');break;case'RENEW':account.renew(action.businessDate,action.actorId,action.maturityDate??'');break;case'MATURE':account.mature(action.businessDate,action.actorId);break;case'SUCCESSION':account.openSuccession(action.businessDate,action.actorId,action.caseReference??'');break;case'CLOSE':account.close(action.businessDate,action.actorId)}await this.repository.save(account.snapshot(),account.pendingEvents(),idempotencyKey);return account.snapshot()}}
