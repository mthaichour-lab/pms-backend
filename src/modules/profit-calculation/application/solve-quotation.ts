import{solveQuotationTarget,type SolveQuotationInput}from'../domain/quotation-solver.js';export type QuotationBasisType='GLOBAL_POOL'|'FINANCING_TYPE'|'DESIGNATED_FINANCING'|'CUSTOMER'|'SECTOR';export interface SolveQuotationCommand{placementAmount:string;targetNetRatePercent:string;basis:{type:QuotationBasisType;reference?:string}}export interface QuotationBasisRepository{loadCertifiedInputs(command:SolveQuotationCommand):Promise<Omit<SolveQuotationInput,'placementAmount'|'targetNetRatePercent'|'designatedBasis'>>}export class SolveQuotation{constructor(private readonly basis:QuotationBasisRepository){}async execute(command:SolveQuotationCommand){if(command.basis.type!=='GLOBAL_POOL'&&!command.basis.reference?.trim())throw new TypeError('Selected quotation basis requires a reference');const certified=await this.basis.loadCertifiedInputs(command);return solveQuotationTarget({...certified,placementAmount:command.placementAmount,targetNetRatePercent:command.targetNetRatePercent,designatedBasis:command.basis.type==='DESIGNATED_FINANCING'})}}
