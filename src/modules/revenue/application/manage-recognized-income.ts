import{validateIncomeAdjustment,validateRecognizedIncome,type IncomeAdjustment,type RecognizedIncome}from'../domain/recognized-income.js';export interface RecognizedIncomeRepository{importIncome(income:RecognizedIncome):Promise<'CREATED'|'EXISTING'>;adjust(adjustment:IncomeAdjustment):Promise<void>;list(poolId:string,businessDate:string):Promise<readonly RecognizedIncome[]>}export class ManageRecognizedIncome{constructor(private readonly repository:RecognizedIncomeRepository){}async import(income:RecognizedIncome){validateRecognizedIncome(income);return{income,status:await this.repository.importIncome(income)}}async adjust(adjustment:IncomeAdjustment){validateIncomeAdjustment(adjustment);await this.repository.adjust(adjustment);return adjustment}list(poolId:string,businessDate:string){return this.repository.list(poolId,businessDate)}}
