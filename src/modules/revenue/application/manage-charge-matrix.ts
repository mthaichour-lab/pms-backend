import{evaluatePoolCharges,validateChargePolicy,type ChargeEvaluation,type ChargePolicy,type PoolCharge}from'../domain/charge-policy.js';export interface ChargeMatrixRepository{savePolicy(policy:ChargePolicy,actorId:string):Promise<void>;importCharge(charge:PoolCharge):Promise<'CREATED'|'EXISTING'>;findCharges(poolId:string,businessDate:string):Promise<readonly PoolCharge[]>;effectivePolicies(businessDate:string):Promise<readonly ChargePolicy[]>}export class ManageChargeMatrix{constructor(private readonly repository:ChargeMatrixRepository){}async configure(policy:ChargePolicy,actorId:string){validateChargePolicy(policy);await this.repository.savePolicy(policy,actorId);return policy}async importCharge(charge:PoolCharge){return{charge,status:await this.repository.importCharge(charge)}}async evaluate(poolId:string,businessDate:string):Promise<ChargeEvaluation>{const[charges,policies]=await Promise.all([this.repository.findCharges(poolId,businessDate),this.repository.effectivePolicies(businessDate)]);return evaluatePoolCharges(charges,policies)}}
