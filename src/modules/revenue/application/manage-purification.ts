import{purificationStatement,validatePurificationCase,type PurificationCase}from'../domain/purification.js';export interface PurificationRepository{save(item:PurificationCase):Promise<void>;document(id:string,beneficiaryId:string,decisionReference:string):Promise<void>;pay(id:string,amount:string,evidenceId:string):Promise<void>;period(poolId:string,from:string,to:string):Promise<{cases:readonly PurificationCase[];openingCarry:string}>}export class ManagePurification{constructor(private readonly repository:PurificationRepository){}async identify(item:PurificationCase){validatePurificationCase(item);await this.repository.save(item);return item}async document(id:string,beneficiary:string,decision:string){if(!beneficiary.trim()||!decision.trim())throw new TypeError('Beneficiary and Sharia decision are required');await this.repository.document(id,beneficiary,decision)}async pay(id:string,amount:string,evidenceId:string){if(!evidenceId.trim())throw new TypeError('Payment evidence is required');await this.repository.pay(id,amount,evidenceId)}async statement(poolId:string,from:string,to:string){const data=await this.repository.period(poolId,from,to);return{...purificationStatement(data.cases,data.openingCarry),cases:data.cases}}}
