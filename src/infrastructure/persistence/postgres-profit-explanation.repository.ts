import type{Pool}from'pg';import type{ProfitExplanationRepository}from'../../modules/reporting/application/query-profit-explanation.js';import type{ProfitExplanationOutput}from'../../modules/profit-calculation/domain/profit-explanation.js';
export class PostgresProfitExplanationRepository implements ProfitExplanationRepository{constructor(private readonly pool:Pick<Pool,'query'>){}async published(runId:string,accountId:string){const result=await this.pool.query<{output:ProfitExplanationOutput;output_checksum_sha256:string}>(`SELECT explanation.output,explanation.output_checksum_sha256 FROM calculation.profit_explanation_output explanation JOIN calculation.run run USING(run_id)WHERE explanation.run_id=$1::uuid AND explanation.account_id=$2::uuid AND run.status IN('POSTED','ARCHIVED')`,[runId,accountId]);const row=result.rows[0];return row?{output:row.output,outputChecksumSha256:row.output_checksum_sha256}:undefined;}}
