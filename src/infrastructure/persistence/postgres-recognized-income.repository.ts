import type{Pool}from'pg';import type{RecognizedIncomeRepository}from'../../modules/revenue/application/manage-recognized-income.js';import type{IncomeAdjustment,RecognizedIncome}from'../../modules/revenue/domain/recognized-income.js';export class PostgresRecognizedIncomeRepository implements RecognizedIncomeRepository{constructor(private readonly pool:Pool){}async importIncome(value:RecognizedIncome){const result=await this.pool.query(`INSERT INTO revenue.recognized_income(income_id,source_system,source_reference,asset_id,pool_id,business_date,currency_code,amount,cash_status,realization_status,income_type)VALUES($1::uuid,$2,$3,$4::uuid,$5,$6::date,$7,$8::numeric,$9,$10,$11)ON CONFLICT(source_system,source_reference,income_type)DO NOTHING`,[value.incomeId,value.sourceSystem,value.sourceReference,value.assetId,value.poolId,value.businessDate,value.currency,value.amount,value.cashStatus,value.realizationStatus,value.incomeType]);if(result.rowCount===1)return'CREATED';const existing=await this.pool.query<{income_id:string;amount:string;pool_id:string}>(`SELECT income_id::text,amount::text,pool_id FROM revenue.recognized_income WHERE source_system=$1 AND source_reference=$2 AND income_type=$3`,[value.sourceSystem,value.sourceReference,value.incomeType]);const row=existing.rows[0];if(row?.income_id!==value.incomeId||Number(row.amount)!==Number(value.amount)||row.pool_id!==value.poolId)throw new Error('Income source replay conflict');return'EXISTING'}async adjust(value:IncomeAdjustment){const approval=await this.pool.query(`SELECT 1 FROM workflow.approval_action WHERE idempotency_key=$1 AND resource_type='RECOGNIZED_INCOME' AND resource_id=$2::text AND result_state='APPROVED'`,[value.approvalId,value.incomeId]);if(approval.rowCount!==1)throw new Error('Income adjustment approval is invalid');await this.pool.query(`INSERT INTO revenue.income_adjustment(adjustment_id,income_id,amount,reason,approval_id,business_date,actor_id)VALUES($1::uuid,$2::uuid,$3::numeric,$4,$5,$6::date,$7)`,[value.adjustmentId,value.incomeId,value.amount,value.reason,value.approvalId,value.businessDate,value.actorId])}async list(poolId:string,businessDate:string){const result=await this.pool.query<any>(`SELECT income_id::text,source_system,source_reference,asset_id::text,pool_id,business_date::text,currency_code,amount::text,cash_status,realization_status,income_type FROM revenue.recognized_income WHERE pool_id=$1 AND business_date=$2::date ORDER BY source_system,source_reference`,[poolId,businessDate]);return result.rows.map((row):RecognizedIncome=>({incomeId:row.income_id,sourceSystem:row.source_system,sourceReference:row.source_reference,assetId:row.asset_id,poolId:row.pool_id,businessDate:row.business_date,currency:row.currency_code,amount:row.amount,cashStatus:row.cash_status,realizationStatus:row.realization_status,incomeType:row.income_type}))}}
